<?php
/**
 * Singles Manager config
 *
 * Copy this file to your project's config/ directory as `singles-manager.php`
 * and adjust as needed.
 */

return [
    /**
     * Section handles (or UIDs) whose single entry edit form should have the
     * right-hand meta sidebar (slug, post date, authors, etc.) hidden entirely.
     *
     * Useful for "settings"-style singles where the metadata panel is clutter.
     *
     * Example:
     *   'hideSidebarSections' => ['generalSettings', 'seoSettings'],
     */
    'hideSidebarSections' => [],
];
